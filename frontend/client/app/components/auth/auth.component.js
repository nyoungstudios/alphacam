import template from './auth.html';
import controller from './auth.controller';
import './auth.scss';

let authComponent = {
  bindings: {},
  template,
  controller
};

export default authComponent;
